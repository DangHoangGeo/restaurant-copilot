# For customer page
1. After a new order is placed, the status of table still be available -> change to unavailable. Similarly, when order session is completed or cancelled, change back to available. Also remember to check table status when creating new order session.
2. 

## For owner, staff side page.
1. Not implement role check to hide some features (delete, edit) yet.

2. For restaurant setting: remove contact info field as already have phone and email field. Add tax field with default rate 10 %.

3. Orders page is too basic, need to improve:
  2.1 Even if today don't have order, owner able to select date range to view order list with detail.
  2.2 Grid view: Always show active order, new order on top, user able to click to view detail. show table name, order time, total price, status, number of guess.
  2.3 List view: Show by order items with table name, ordered time, quantity, size, toppings and status

4. Cuppon and promotion for restaurant (not implement yet).
